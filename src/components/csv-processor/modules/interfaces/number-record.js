// src/components/csv-processor/modules/interfaces/number-record.js
// Este archivo es conceptual para definir la "forma" de los datos.
// En JavaScript puro, no hay tipado estricto como en TypeScript,
// pero sirve como documentación.

/**
 * @typedef {Object} NumberRecord
 * @property {string} number - El número de 10 dígitos.
 */

// No se exporta nada, es solo para referencia o JSDoc.